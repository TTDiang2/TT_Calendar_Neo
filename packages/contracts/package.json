{
  "name": "@tt-calendar/contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Zod 契约：全栈单一类型来源（DB 校验 / 领域实体 / 前端 props 共用）",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
